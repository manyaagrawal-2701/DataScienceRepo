# Write function that takes list and returns its length
list=[1,2,3,4,5] # created a list
def length(l1): # taken list as a parameter
    return len(l1) # returning length of list using len function
print(length(list))